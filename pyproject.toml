[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "smart-bmi-analyzer"
version = "1.0.0"
description = "Smart BMI Analyzer API"
requires-python = ">=3.12"
dependencies = []
